using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}


// Will cover AI of enemy movement
// Need to bring in turn variable from PlayerController
// Movement is similar to player, just opposite and automated